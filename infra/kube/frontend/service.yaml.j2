apiVersion: v1
kind: Service
metadata:
  name: {{ spezi_service_name }}-frontend-service
  namespace: {{ spezi_service_name }}
spec:
  selector:
    app: {{ spezi_service_name }}-frontend
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 80
  type: ClusterIP